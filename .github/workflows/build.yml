name: "Build"

on:
  push:
    branches: [main]
    paths-ignore: ["**/*.md"]
  pull_request:
    branches: [main]
    paths-ignore: ["**/*.md"]
  workflow_dispatch:

jobs:
  call:
    uses: jellyfin/jellyfin-meta-plugins/.github/workflows/build.yaml@facf0dc93920880bb311641b5117eae2df8cca63 # master
    with: 
      dotnet-target: 'net10.0'
      dotnet-version: '10.0.x'
