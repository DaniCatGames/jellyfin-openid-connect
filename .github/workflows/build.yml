name: "Build"

on:
  push:
    branches: [main]
    paths-ignore: ["**/*.md"]
  pull_request:
    branches: [main]
    paths-ignore: ["**/*.md"]
  workflow_dispatch:

jobs:
  call:
    uses: jellyfin/jellyfin-meta-plugins/.github/workflows/build.yaml@601009caac9d7e9f891ef8ed8bb9446490b7c1c1 # master
    with: 
      dotnet-target: 'net10.0'
      dotnet-version: '10.0.x'
