name: "Build"

on:
  push:
    branches: [main]
    paths-ignore: ["**/*.md"]
  pull_request:
    branches: [main]
    paths-ignore: ["**/*.md"]
  workflow_dispatch:

jobs:
  call:
    uses: jellyfin/jellyfin-meta-plugins/.github/workflows/build.yaml@3abd0cb4321d944d4d469b731ef98021aaa15346 # master
    with: 
      dotnet-target: 'net10.0'
      dotnet-version: '10.0.x'
