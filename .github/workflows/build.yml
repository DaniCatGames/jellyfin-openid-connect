name: "Build"

on:
  push:
    branches: [main]
    paths-ignore: ["**/*.md"]
  pull_request:
    branches: [main]
    paths-ignore: ["**/*.md"]
  workflow_dispatch:

jobs:
  call:
    uses: jellyfin/jellyfin-meta-plugins/.github/workflows/build.yaml@c5847d4d5a8a577632723fe0ff120d9ddcbc5d09 # master
    with: 
      dotnet-target: 'net10.0'
      dotnet-version: '10.0.x'
