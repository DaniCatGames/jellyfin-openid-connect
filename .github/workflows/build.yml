name: "Build"

on:
  push:
    branches: [main]
    paths-ignore: ["**/*.md"]
  pull_request:
    branches: [main]
    paths-ignore: ["**/*.md"]
  workflow_dispatch:

jobs:
  call:
    uses: jellyfin/jellyfin-meta-plugins/.github/workflows/build.yaml@90939233af180b897916325bcb36ff05988d6d40 # master
    with: 
      dotnet-target: 'net10.0'
      dotnet-version: '10.0.x'
