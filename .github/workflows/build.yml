name: "Build"

on:
  push:
    branches: [main]
    paths-ignore: ["**/*.md"]
  pull_request:
    branches: [main]
    paths-ignore: ["**/*.md"]
  workflow_dispatch:

jobs:
  call:
    uses: jellyfin/jellyfin-meta-plugins/.github/workflows/build.yaml@72779f42623032ec7f12a9c30f91d78f66be3fab # master
    with: 
      dotnet-target: 'net10.0'
      dotnet-version: '10.0.x'
